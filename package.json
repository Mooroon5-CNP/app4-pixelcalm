{
  "name": "app4-pixelcalm",
  "version": "1.0.0",
  "description": "Pixel Calm — write your thoughts, watch them float away into space",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "eslint src/",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --ci --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0",
    "express": "^4.18.2",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "eslint": "^8",
    "jest": "^29",
    "jest-junit": "^16",
    "supertest": "^6"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "jest-junit": {
    "outputDirectory": ".",
    "outputName": "test-results.xml"
  }
}
